# Runtime dependencies — fully pinned (direct + transitive) so a rebuild can
# never pull an untested Werkzeug/Jinja2/etc. Regenerate with:
#   pip install Flask==3.1.1 gunicorn==23.0.0 && pip freeze
# Flask 3.1.1 carries the fix for CVE-2025-47278 (SECRET_KEY_FALLBACKS signing).
Flask==3.1.1
gunicorn==23.0.0

# Transitive (pinned explicitly):
Werkzeug==3.1.8
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.4.2
blinker==1.9.0
colorama==0.4.6
